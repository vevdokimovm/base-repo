# tests/ — регрессионный набор инфраструктуры

Тесты на то, что ломается молча и дорого. Сейчас закрыт `templates/deploy.sh` —
единственный деплойер системы.

## Запуск

```bash
bash tests/test_deploy.sh        # из корня base-repo
```

Ноль зависимостей: ни сети, ни GitHub-токена, ни реальных реп.
Вместо GitHub — локальные bare-репы (`REMOTE_BASE`), вместо `gh` — стаб `tests/bin/gh`
с журналом вызовов. Код возврата `0` — зелено, `1` — есть падения.

## Что покрыто (48 кейсов, 162 проверки)

| # | Кейс | Зачем |
|---|---|---|
| 1 | Новая репа: создание → коммит → тег → релиз → ассет | базовый цикл целиком |
| 2 | Серия версий: каждый тег на СВОЁМ дереве | регресс PIT-014 (stat-кэш git) |
| 3 | Идемпотентность | повторный прогон безопасен |
| 4 | Нейминг: подчёркивания принимаются, помечаются | `43` — легаси не ломаем |
| 5 | `__MACOSX`/`.DS_Store` не ломают пуш | регресс PIT-015 |
| 6 | Хвост ` copy` / `(1)` в имени | реальные файлы из Downloads |
| 7 | Битый архив без README | репа НЕ разрушена |
| 8 | `VERSION` ≠ версии в имени | стоп по версии |
| 9 | `DRY=1` | ни одного изменения |
| 10 | Заглушка чинится, осмысленное описание — нет | `21-revision-protocol` |
| 11 | CHANGELOG без тезиса | предупреждение, публикация идёт |
| 12 | Тег на чужом дереве → fail loud | не чиним молча |
| 13 | `ONLY` / `SKIP` | фильтры |
| 14 | repos-map: авто-регистрация новой репы | без дублей |
| 15 | Правка карты не теряется во временном клоне | регресс боевого прогона |
| 16 | Шесть исторических вариантов имени архива | `43` — старое не выпадает |
| 17 | Двухчастная версия `v2.7` → `2.7.0` | историческая ошибка |
| 18 | SemVer-сортировка: 2.9.0 < 2.10.0 | не строковое сравнение |
| 19 | Плоская упаковка без обёртки | реальный случай |
| 20 | Двойная обёртка | реальный случай |
| 21 | Мало файлов → репа не создаётся | регресс «репа-сирота» |
| 22 | Битый zip не роняет батч | устойчивость |
| 23 | `BACKFILL` | старые версии |
| 24 | `FORCE` | заморозка описаний |
| 25 | `ASSETS_ONLY` | дозаливка ассетов |
| 26 | UTF-8: кириллица и em-dash | парсер на python |
| 27 | Пустая папка → понятное сообщение | UX |
| 28 | `NO_COLOR` — чистый текст в пайп | цветной вывод |
| 29 | Пустая репа без коммитов | краевой случай |
| 30 | Несколько реп за прогон | батч |
| 31 | Вложенная репа со своим VERSION | регресс на реальном архиве |
| 32 | Служебные архивы из чата игнорируются | шум из реального Downloads |
| 33 | Вариантный постфикс только для VARIANT_REPOS | `finpilot_..._intl` |
| 34 | Не-UTF-8 имена внутри архива | регресс `Illegal byte sequence` |
| 35 | `REPO_MAP`: архив едет в репу с другим именем | `finpilot_*` → `personal-finance-dss` |
| 36 | Аудит: не версионные архивы перечислены | ничего не пропадает молча |
| 37 | `Latest` при backfill не сбивается на старую версию | порядок релизов |
| 38 | macOS-хвост ` 1` после версии = дубликат | повторная загрузка в браузере |
| 39 | Имя без версионного токена — тихий аудит | лишний жёлтый шум |
| 40 | repos-map обновляется сама, без переменных | одна команда должна делать всё |
| 41 | CHANGELOG в подпапке находится | **6 пустых релизов base-repo** |
| 42 | CHANGELOG глубоко в дереве | тот же класс |
| 43 | Нет секции → релиз НЕ создаётся | пустышки на GitHub |
| 44 | Старые релизы чинятся в `AUDIT=1` | ассет из тега, тезис, тело |
| 45 | `FORCE` без секции в CHANGELOG | падение всех таких версий |
| 46 | Нормализация шапки `## vX.Y.Z` → `## [X.Y.Z]` | разнобой в теле релиза |
| 47 | Дефолт не запускает ревизию | два часа на каждый прогон |
| 48 | `ALL_REPOS=1` берёт репы из `repos-map` | репу без архива было не проверить |

## Правило

Поймал баг в деплойере — **сначала тест, который его воспроизводит**, потом фикс.
Кейсы 2, 5, 15, 21 появились именно так: это пойманные баги, а не гипотезы.
